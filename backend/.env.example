# Backend Server Port (optional, defaults to 5000 in server.ts)
# PORT=5000

# Neon PostgreSQL Database Connection URL (using Neon HTTP driver format)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

#(e.g., http://localhost:5173 for default Vite dev server) - this is where the user is redirected after successful login.
FRONTEND_URL="http://localhost:5173"

# Lucia Session Cookie Secure Flag (set to true in production with HTTPS)
# NODE_ENV=production

# --- Google OAuth --- 
# Get these from Google Cloud Console -> APIs & Services -> Credentials
GOOGLE_CLIENT_ID="YOUR_GOOGLE_CLIENT_ID"
GOOGLE_CLIENT_SECRET="YOUR_GOOGLE_CLIENT_SECRET"
# This MUST match the authorized redirect URI in Google Cloud Console
# Typically http://localhost:5000/api/auth/google/callback during development
GOOGLE_REDIRECT_URI="YOUR_BACKEND_GOOGLE_CALLBACK_URL"

# --- Optional: Other OAuth Providers ---
# GITHUB_CLIENT_ID="YOUR_GITHUB_CLIENT_ID"
# GITHUB_CLIENT_SECRET="YOUR_GITHUB_CLIENT_SECRET"
# GITHUB_REDIRECT_URI="YOUR_BACKEND_GITHUB_CALLBACK_URL"
